import angular from 'angular'; export default angular.module('templates', []).run(['$templateCache', function($templateCache) {$templateCache.put('users/users.template.html','<md-bottom-sheet class="md-list md-has-header">\n\n    <md-subheader>\n        Contact <span class="name">{{ $ctrl.user.name }}</span>:\n    </md-subheader>\n\n    <md-list>\n        <md-item ng-repeat="item in $ctrl.items">\n            <md-button ng-click="$ctrl.performAction(item)">\n                <md-icon md-svg-icon="{{ item.icon }}"></md-icon>\n                {{item.name}}\n            </md-button>\n        </md-item>\n    </md-list>\n\n</md-bottom-sheet>');}]);